# Step 6 - Sample Data Preparation

Based strictly on the outputs from previous steps in the [output folder](../../../../outputs/)

Save to: `outputs\06-sample-data-G02.sql`

## Output Document Structure

The output file must contain the following sections.

### 1. Valid Sample Data

#### 1.1 Insert sample data for primary tables

Primary tables are those that do not depend on foreign keys.

Generate realistic data for:
- User
- Space
- Facility

Recommended minimum records:

| Table | Minimum Records |
|-------|----------------|
| User | 6 |
| Space | 5 |
| Facility | 6 |

Use the following command:
```sql
insert into [table_name]
(
    [attribute_1],
    [attribute_2],
    ...
)
values
(
    [value_1],
    [value_2],
    ...
)
```

**NOTES**
1. Generate realistic university-related data.
2. Use valid predefined values only.
3. Primary keys must be unique.
4. Do not violate any implemented constraints.
5. User roles must include:
    - student
    - lecturer
    - teaching_assistant
    - facility_staff
    - facility_manager
    - department_administrator
6. Space types should include:
    - auditorium
    - classroom
    - computer_laboratory
    - meeting_room
    - student_workspace

#### 1.2. Insert sample data for dependent tables

Dependent tables are those that contain foreign keys.

Generate realistic data for:
- SpaceFacility
- BookingRequest
- MaintenanceRecord

Recommended minimum records:

| Table | Minimum Records |
|-------|----------------|
| SpaceFacility | 8 |
| BookingRequest | 8 |
| MaintenanceRecord | 4 |

Use the following command:
```sql
insert into [table_name]
(
    [attribute_1],
    [attribute_2],
    ...
)
values
(
    [value_1],
    [value_2],
    ...
)
```

**NOTES**
1. Parent records must already exist.
2. All foreign key values must reference existing records.
3. Booking and maintenance dates must be realistic.
4. Booking lifecycle examples should include:
    - pending
    - approved
    - rejected
    - checked_in
    - completed
5. Maintenance lifecycle examples should include:
    - reported
    - in_progress
    - completed

### 2. Exceptional Test Cases

Generate invalid data to verify constraint enforcement.

Each test case must target exactly one constraint or business rule.

Create a separate SQL block for every test case.

Format:

```sql
-- TC-XX: Test description
-- Expected Result: FAIL

insert into [table_name]
(
    [attribute_1],
    [attribute_2],
    ...
)
values
(
    [value_1],
    [value_2],
    ...
)
```

Requirements:

- Keep test cases independent.
- Do not reuse failed records in subsequent tests.
- Clearly indicate the expected failure.

---

## SQL Generation Requirements

1. Use Microsoft SQL Server syntax only.
2. Use explicit column names for every INSERT statement.
3. Do not use `SELECT *`
4. Do not use autogenerated IDs
5. Do not use placeholder values
6. Do not use random meaningless values

---

## Validation Rules

1. Every table must have valid sample data.
2. Every implemented constraint must be tested.
3. Every implemented trigger must be tested.
4. Every implemented business rule must be tested.
5. Every exceptional case must be independent.
6. The SQL file must be executable without modification.